const { partner_scene } = require('./partner.scene.js');
const { job_seeker_scene } = require('./job_seeker.scene.js');
const { employee_search_scene } = require('./employee_search.scene.js');
const { mentor_search_scene } = require('./mentor_search.scene.js');
const { apprentice_search_scene } = require('./apprentice_search.scene.js');

module.exports = {
    partner_scene,
    job_seeker_scene,
    employee_search_scene,
    mentor_search_scene,
    apprentice_search_scene
};
